services:
  jellyfin:
    image: jellyfin/jellyfin:10.10
    container_name: homelab-jellyfin
    restart: unless-stopped
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - /Volumes/homelab_data/homelab/data/jellyfin:/config
      - /Volumes/homelab_data/homelab/data/jellyfin/cache:/cache
      - /Volumes/homelab_data/homelab/data/media:/data/media
    networks:
      - homelab
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jellyfin.rule=Host(`jellyfin.${DOMAIN_BASE}`)"
      - "traefik.http.routers.jellyfin.entrypoints=websecure"
      - "traefik.http.routers.jellyfin.tls=true"
      - "traefik.http.routers.jellyfin.tls.certresolver=letsencrypt"
      - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"
